services:
  securestore-preview:
    build:
      context: .
      dockerfile: Dockerfile
    image: securestore:1.0.0
    container_name: securestore-preview
    ports:
      - "4173:4173"
    environment:
      NODE_ENV: production
